<!-- 计划列表次级窗口 -->
<Window x:Class="AutoClicker.App.Views.PlanListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="计划列表" Width="500" Height="400"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner"
        Background="{StaticResource ColorBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ScheduledTasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,8" Margin="0,0,0,4" CornerRadius="6"
                                Background="{StaticResource ColorSurfaceLow}"
                                BorderBrush="{StaticResource ColorBorderLight}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock FontSize="13" FontWeight="SemiBold"
                                               Text="{Binding TaskSummary}"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="✕" Width="28" Height="24"
                                        Padding="0" FontSize="11"
                                        Style="{StaticResource ButtonSecondary}"
                                        Command="{Binding DataContext.RemoveScheduledTaskCommand,
                                                  RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="1" Content="关闭" Height="36" Margin="0,12,0,0"
                Style="{StaticResource ButtonSecondary}"
                Click="Close_Click"/>
    </Grid>
</Window>
