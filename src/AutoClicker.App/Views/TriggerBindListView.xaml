<!-- 快捷键绑定列表次级窗口 -->
<Window x:Class="AutoClicker.App.Views.TriggerBindListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="快捷键绑定列表" Width="480" Height="400"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner"
        Background="{StaticResource ColorBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding TriggerBindings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,8" Margin="0,0,0,4" CornerRadius="6"
                                Background="{StaticResource ColorSurfaceLow}"
                                BorderBrush="{StaticResource ColorBorderLight}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <!-- 键名徽章 -->
                                <Border Grid.Column="0" Padding="8,3" MinWidth="56"
                                        CornerRadius="4" HorizontalAlignment="Center"
                                        Background="{StaticResource ColorPrimaryLight}">
                                    <TextBlock Text="{Binding KeyDisplay}" FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource ColorPrimary}"
                                               HorizontalAlignment="Center"/>
                                </Border>
                                <!-- 详情 -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock FontSize="13" FontWeight="SemiBold">
                                        <Run Text="{Binding PositionDisplay, Mode=OneWay}"/>
                                        <Run Text="  "/>
                                        <Run Text="{Binding ClickModeDisplay, Mode=OneWay}"
                                             Foreground="{StaticResource ColorTextSecondary}"/>
                                    </TextBlock>
                                </StackPanel>
                                <!-- 删除按钮 -->
                                <Button Grid.Column="3" Content="✕" Width="28" Height="24"
                                        Padding="0" FontSize="11"
                                        Style="{StaticResource ButtonSecondary}"
                                        Command="{Binding DataContext.RemoveTriggerBindingCommand,
                                                  RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="1" Content="关闭" Height="36" Margin="0,12,0,0"
                Style="{StaticResource ButtonSecondary}"
                Click="Close_Click"/>
    </Grid>
</Window>
