<!--
  应用全局资源 — 简约浅色主题，设计灵感来自 Indigo 品牌色系
  设计令牌（色彩、字体） + 全局控件样式
-->
<Application x:Class="AutoClicker.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:conv="clr-namespace:AutoClicker.App.Converters">
    <Application.Resources>

        <!-- ═══ 色彩令牌 ═══ -->
        <SolidColorBrush x:Key="ColorPrimary" Color="#6366F1" />
        <SolidColorBrush x:Key="ColorPrimaryHover" Color="#5558E6" />
        <SolidColorBrush x:Key="ColorPrimaryLight" Color="#EEF2FF" />
        <SolidColorBrush x:Key="ColorOnPrimary" Color="#FFFFFF" />
        <SolidColorBrush x:Key="ColorBackground" Color="#F5F6FA" />
        <SolidColorBrush x:Key="ColorSurface" Color="#FFFFFF" />
        <SolidColorBrush x:Key="ColorMuted" Color="#F3F4F6" />
        <SolidColorBrush x:Key="ColorAccent" Color="#E8EAF6" />
        <SolidColorBrush x:Key="ColorTextPrimary" Color="#1E1E2E" />
        <SolidColorBrush x:Key="ColorTextSecondary" Color="#6B7280" />
        <SolidColorBrush x:Key="ColorTextMuted" Color="#9CA3AF" />
        <SolidColorBrush x:Key="ColorBorder" Color="#E5E7EB" />
        <SolidColorBrush x:Key="ColorBorderLight" Color="#F0F0F5" />
        <SolidColorBrush x:Key="ColorSuccess" Color="#10B981" />
        <SolidColorBrush x:Key="ColorDanger" Color="#EF4444" />

        <!-- 品牌渐变（不可直接绑定，通过模板使用） -->
        <LinearGradientBrush x:Key="BrandGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366F1" Offset="0" />
            <GradientStop Color="#818CF8" Offset="1" />
        </LinearGradientBrush>

        <!-- ═══ 字体令牌 ═══ -->
        <FontFamily x:Key="FontPrimary">Segoe UI</FontFamily>
        <system:Double x:Key="FontSizeCaption">11</system:Double>
        <system:Double x:Key="FontSizeSm">12</system:Double>
        <system:Double x:Key="FontSizeBody">13</system:Double>
        <system:Double x:Key="FontSizeHeading">14</system:Double>
        <system:Double x:Key="FontSizeLg">16</system:Double>

        <!-- ═══ 全局样式 ═══ -->

        <!-- TextBlock 默认 -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}" />
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextPrimary}" />
        </Style>

        <!-- 次要文字 -->
        <Style x:Key="TextSecondary" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource FontSizeSm}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextMuted}" />
        </Style>

        <!-- 提示文字 -->
        <Style x:Key="TextHint" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource FontSizeSm}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextSecondary}" />
        </Style>

        <!-- ══════ GroupBox → Section 卡片 ══════ -->
        <Style TargetType="GroupBox">
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Margin="{TemplateBinding Margin}">
                            <StackPanel>
                                <!-- 标题行：渐变强调条 + 文字 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border Width="4" Height="16" CornerRadius="2"
                                            Background="{StaticResource BrandGradient}"
                                            VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="{TemplateBinding Header}"
                                               FontSize="{StaticResource FontSizeCaption}"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource ColorTextSecondary}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                                <!-- 卡片内容区 -->
                                <Border Background="{StaticResource ColorSurface}"
                                        CornerRadius="12"
                                        BorderBrush="{StaticResource ColorBorder}"
                                        BorderThickness="1"
                                        Padding="12">
                                    <ContentPresenter />
                                </Border>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══════ RadioButton → 圆圈+圆点 ══════ -->
        <Style TargetType="RadioButton">
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}" />
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextSecondary}" />
            <Setter Property="Padding" Value="0,2" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="Transparent" Padding="0,3" Cursor="Hand">
                            <StackPanel Orientation="Horizontal">
                                <!-- 外圆 -->
                                <Grid Width="18" Height="18" VerticalAlignment="Center">
                                    <Ellipse Width="18" Height="18"
                                             x:Name="OuterCircle"
                                             Stroke="{StaticResource ColorTextMuted}"
                                             StrokeThickness="1.5"
                                             Fill="Transparent" />
                                    <!-- 内点 -->
                                    <Ellipse Width="9" Height="9"
                                             x:Name="Dot"
                                             Fill="{StaticResource ColorPrimary}"
                                             Opacity="0" />
                                </Grid>
                                <ContentPresenter VerticalAlignment="Center"
                                                  Margin="8,0,0,0"
                                                  RecognizesAccessKey="True" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterCircle" Property="Stroke"
                                        Value="{StaticResource ColorPrimary}" />
                                <Setter TargetName="OuterCircle" Property="Fill"
                                        Value="{StaticResource ColorPrimaryLight}" />
                                <Setter TargetName="Dot" Property="Opacity" Value="1" />
                                <Setter Property="Foreground"
                                        Value="{StaticResource ColorTextPrimary}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterCircle" Property="Stroke"
                                        Value="{StaticResource ColorPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══════ TextBox → 圆角输入框 ══════ -->
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}" />
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextPrimary}" />
            <Setter Property="CaretBrush" Value="{StaticResource ColorPrimary}" />
            <Setter Property="BorderBrush" Value="{StaticResource ColorBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource ColorAccent}" />
            <Setter Property="Height" Value="34" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <Border BorderBrush="{StaticResource ColorDanger}" BorderThickness="1.5" CornerRadius="10">
                            <AdornedElementPlaceholder/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush"
                                        Value="{StaticResource ColorPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══════ Button → 圆角品牌按钮 ══════ -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}" />
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="Background" Value="{StaticResource ColorPrimary}" />
            <Setter Property="Foreground" Value="{StaticResource ColorOnPrimary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 次要按钮（灰色背景） -->
        <Style x:Key="ButtonSecondary" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource ColorAccent}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextSecondary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource ColorBorder}" />
        </Style>

        <!-- ══════ ComboBox → 圆角下拉框 ══════ -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="Border" Background="Transparent" CornerRadius="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="22"/>
                    </Grid.ColumnDefinitions>
                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="{StaticResource ColorTextSecondary}"
                          StrokeThickness="1.5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
            </Border>
        </ControlTemplate>

        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{StaticResource FontPrimary}" />
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
            <Setter Property="Foreground" Value="{StaticResource ColorTextPrimary}" />
            <Setter Property="Background" Value="{StaticResource ColorAccent}" />
            <Setter Property="BorderBrush" Value="{StaticResource ColorBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="34" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="12,0,28,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border" Background="{TemplateBinding Background}"
                                    CornerRadius="10"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource ComboBoxToggleButton}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press" HorizontalAlignment="Stretch"/>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Margin="{TemplateBinding Padding}"
                                              IsHitTestVisible="False"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border x:Name="PopupBorder" Background="{StaticResource ColorSurface}"
                                        CornerRadius="10"
                                        BorderBrush="{StaticResource ColorBorder}"
                                        BorderThickness="1" Margin="0,2,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource ColorPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource ColorPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem 圆角选中 -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="Transparent" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ColorPrimaryLight}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ColorAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 转换器 -->
        <conv:BoolToStatusConverter x:Key="BoolToStatus"/>
        <conv:BoolToColorConverter x:Key="BoolToColor"/>
        <conv:BoolToInverseConverter x:Key="BoolInverse"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>

        <!-- 新增功能引用的资源别名（向后兼容） -->
        <SolidColorBrush x:Key="ColorPrimaryContainer" Color="#6366F1" />
        <SolidColorBrush x:Key="ColorSurfaceLow" Color="#F5F6FA" />
        <SolidColorBrush x:Key="ColorSurfaceHigh" Color="#E8EAF6" />
        <DropShadowEffect x:Key="ShadowCard" BlurRadius="8" ShadowDepth="2" Opacity="0.04" />

    </Application.Resources>
</Application>
