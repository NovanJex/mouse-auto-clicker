# 安全策略

## 支持的版本

| 版本 | 安全更新 |
|------|---------|
| v1.2.1 | ✅ 当前支持 |
| < v1.2.1 | ❌ 不再支持 |

## 报告漏洞

如果你发现安全漏洞，**请不要在公开的 Issue 中报告**。

请通过以下方式私下报告：
- 在 GitHub 上向项目维护者发送私信
- 邮件发送至项目维护者邮箱（如有）

我们将在 48 小时内确认收到报告，并在 30 天内提供修复方案。

## 安全设计

本项目采用以下安全设计原则：

- **最小权限**：应用以 `asInvoker` 权限运行，不请求管理员权限
- **API 隔离**：所有 Win32 API 调用在服务层封装，不暴露给 UI 层
- **注入事件过滤**：键盘/鼠标钩子过滤 `LLKHF_INJECTED` / `LLMHF_INJECTED` 标志位，防止恶意软件注入
- **输入验证**：所有用户输入（坐标、延迟、CPS）在服务层进行边界检查
- **单实例运行**：Mutex 防止多开导致的资源竞争

## 依赖项

| 依赖 | 用途 |
|------|------|
| CommunityToolkit.Mvvm 8.x | MVVM 源生成器 |
| Microsoft.Extensions.DependencyInjection 8.x | DI 容器 |
| .NET 8 Runtime | 运行时 |
